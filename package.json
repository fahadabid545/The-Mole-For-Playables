{
  "name": "jungle-mole",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:playables": "cross-env VITE_BUILD_TARGET=playables vite build",
    "build:store": "cross-env VITE_BUILD_TARGET=store vite build",
    "preview": "vite preview --port 5000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
